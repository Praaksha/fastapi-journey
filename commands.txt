for installing FastAPI - pip install fastapi uvicorn
for running code - uvicorn main:app --reload  # main - file name, app - fast api object




GIT commands:
git init - to initialise git in local
git status - to check the current status of all files in the current folder
git add . - to add files into staging
git commit -m "commit name" - to commit the changes
git remote add origin git_path - connect to the git repo
git branch -M main - to change the branch to main
git push -u origin main - to push the code into git repopsitory


Dockerfile contents

FROM python:3.12

WORKDIR /app

COPY requirements.txt .

RUN pip install -r requirements.txt

COPY . .

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]


Installation and setup of PostgresSQL
pip install psycopg2-binary


Creating environment variable
pip install python-dotenv
create .env file and add environment variable without spaces, Eg: DB_HOST=myhost
import os
from dotenv import load_dotenv
load_dotenv()
when we want to use env variable get that by os.getenv("DB_HOST)